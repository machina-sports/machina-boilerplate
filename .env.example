# Server-only pod credentials. Never prefix these with NEXT_PUBLIC_.
MACHINA_API_URL=https://your-project.org.machina.gg
# EITHER a pod API key (sent as X-Api-Token)...
MACHINA_API_KEY=
# ...OR a project token JWT - what `machina login` mints (sent as
# X-Project-Token). Set one of the two; the project token wins when both set.
MACHINA_PROJECT_TOKEN=

# Agent selected by the chat UI.
MACHINA_AGENT=machina-assistant-executor

# Optional comma-separated allowlist of workflow names /api/thread/stream may
# stream. The browser can name only these; leaving it empty closes the workflow
# path entirely. The agent path is always pinned to MACHINA_AGENT.
MACHINA_WORKFLOWS=

# Optional white-label settings.
NEXT_PUBLIC_APP_NAME={{APP_NAME}}
NEXT_PUBLIC_BRAND=default
