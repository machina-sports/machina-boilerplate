name: Frontend staging

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/_frontend-deploy.yml
    with:
      environment: staging
    secrets: inherit
